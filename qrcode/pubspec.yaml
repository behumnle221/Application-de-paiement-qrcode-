name: qrcode
description: "Application de paiement par QR code"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1   # Indispensable pour la police Inter
  path_provider: ^2.1.4
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Si plus tard tu veux ajouter des images ou icônes personnalisées
  # assets:
  #   - assets/images/